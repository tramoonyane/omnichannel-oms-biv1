RewriteEngine On

# Existing files/directories are served normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Only route API requests to Slim
RewriteRule ^api(/.*)?$ api.php [QSA,L]